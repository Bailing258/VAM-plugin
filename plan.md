# AllPackagesLinker UI 重构计划

## 1. 目标与边界

本计划只针对 `AllPackagesLinkerBepInEx.cs` 中运行时构建的 Unity UI，目标是在不改变包扫描、链接、依赖解析、Hub 下载、预设应用和场景加载行为的前提下，降低界面拥挤感、误操作概率和后续维护成本。

本轮实现应遵守以下边界：

- 保留桌面与 VR 两种入口、现有收藏/默认保留/分页状态和 `config.tsv` 兼容性。
- 第一阶段不引入外部 UI 框架或资源包，继续使用项目当前可用的 `UnityEngine.UI`，避免 VaM/旧 Unity 运行时兼容风险。
- 不借 UI 重构改写扫描、链接或预设加载算法；业务行为通过现有方法接入新界面。
- 危险操作（删除包、清除软链接）必须继续二次确认，并与日常操作在视觉和空间上隔离。
- 桌面与 VR 使用同一套信息架构，但允许不同密度、字号、卡片列数和可见工具。

## 2. 当前 UI 诊断

### 2.1 根因不是单纯配色，而是信息架构失焦

当前 `BuildPanel()` 一次性创建顶部标题、搜索框、10 个一级分类、收藏二级分类、VR 位置调节、网格列表、20 页页码条、8 个分页/页容量/扫描按钮，以及右侧从预览、原子选择、预设选项、全局设置、应用/加载、收藏/保留、Hub、下载进度到删除操作的完整长面板。高频浏览、上下文操作、全局设置、维护操作和危险操作没有分层，所以用户每次选择内容都面对大量无关控件。

### 2.2 现有布局的具体问题

- 顶栏只有 64 px 高，却同时容纳动态长标题、搜索、10 个标签和关闭按钮；分类标签实际宽度过小，中文易拥挤。
- 左侧 72% / 右侧 28% 的固定比例使右侧三按钮行、长中文标签和 VR 点击目标偏窄；右栏虽可滚动，但所有功能永久存在，形成“操作清单”。
- 同一动作在卡片和详情区重复出现，例如加载/应用、收藏、默认保留、包操作；视觉上无法判断主入口。
- 预设专用控件、脚本操作和场景操作始终同时创建，没有随当前分类与选择类型显隐。
- 4 个全局设置和 VR 调节直接混入主工作流；低频维护选项挤占高频操作空间。
- 分页同时存在首页、末页、前后 1/5/10 页、20 个动态页码和每页 ±8，控制冗余且占两条底部区域。
- 搜索只匹配有限字段，且没有清空、结果数、空状态和当前筛选的明确反馈。
- 列表刷新时部分页面会自动选择当前页第一项，选择状态规则分散在多个 `Refresh*List()` 中，容易造成右侧对象与用户意图不一致。
- `MakeButton()` 只有背景色差异，缺少统一的主按钮、次按钮、幽灵按钮、危险按钮、禁用态和选中态语义。
- 代码层面，UI 状态、布局构建、列表渲染、业务调用和持久化集中在一个约 27 万字符的源文件中；`BuildPanel()` 和多个单行 `Refresh*` 方法使界面修改容易引入状态回归。

## 3. 推荐的目标界面

采用“资源浏览器 + 上下文检查器”的两栏结构，并把低频设置移入独立抽屉/弹层：

1. 顶部应用栏：固定产品名、全局搜索、设置入口、关闭按钮。
2. 左侧浏览区：一级内容导航、按当前内容变化的二级筛选、结果工具条、卡片网格。
3. 右侧检查器：只显示当前选择的预览、关键元数据和该资源可执行的动作。
4. 底部状态栏：任务状态/错误摘要在左，紧凑分页在右；下载时切换为明确的进度与取消操作。
5. 设置抽屉：集中放置启动扫描、自动允许插件、场景前清理、VR 尺寸/距离/高度等低频配置。
6. 危险操作弹层：清除链接与删除真实包必须从“更多/维护”进入并二次确认。

### 3.1 功能层级

- 一级导航：`收藏`、`场景`、`预设`、`服装`、`头发`、`形态`、`脚本`、`资源`、`全部`。将含义模糊的 `其他` 放入 `全部` 的筛选项，减少顶级入口数量。
- 二级筛选：只显示当前一级导航真实支持的选项。例如收藏页显示资源类型；预设页显示本地/包内和预设类型；全部页显示包分类。
- 主动作：根据选择类型只保留一个最可能动作——场景“加载场景”、预设“应用到人物”、脚本“加载到原子”、普通包“链接包”。
- 次动作：收藏、默认保留、仅链接、打开 Hub 等放在主动作下方或更多菜单内。
- 维护动作：重新扫描、清除生成链接、删除包、下载目录与自动化设置统一进入设置/维护区域。

## 4. 成功标准

- 打开面板后，用户无需滚动右栏即可完成“选资源 → 执行主动作”。
- 任一选择类型下，右栏不出现不适用的动作；无选择时不保留上一个资源的可执行按钮。
- 顶部在 1920×1080 参考分辨率下无文字重叠；VR 中所有主要点击目标建议不小于 44×44 参考像素。
- 常规翻页只需上一页/页码/下一页；首页末页可保留为紧凑图标或在页码窗口中提供。
- 用户切换分类、搜索、翻页或刷新后，选择策略一致且不会静默切换为另一资源后执行动作。
- 新 UI 不改变原有配置、收藏和默认保留数据格式，旧数据可直接读取。

## 5. 代码组织与关键文件

### 5.1 建议拆分为 partial class

当前构建响应文件只编译 `AllPackagesLinkerBepInEx.cs`，因此拆分时必须同步更新两个 `.rsp`。推荐最终结构：

- `AllPackagesLinkerBepInEx.cs`：保留插件生命周期、扫描/索引、链接、Hub、预设与场景业务；将类声明改为 `public partial class AllPackagesLinkerBepInEx`。
- `AllPackagesLinker.UI.cs`：面板打开/关闭、整体布局、导航、检查器、设置抽屉、确认弹层以及 UI 刷新入口。
- `AllPackagesLinker.UI.Cards.cs`：包、场景、本地预设、包内预设、服装/头发等卡片渲染与缩略图队列。
- `AllPackagesLinker.UI.Components.cs`：颜色/间距/字号 token，按钮、文本、输入框、切换项、分隔线、空状态等统一组件工厂。
- `_build/refs.rsp` 与 `_build/refs_test.rsp`：在末尾加入上述新增 `.cs` 文件，确保正式和测试构建使用相同源文件集合。

拆分只移动代码，不改变访问级别或业务逻辑；先让拆分版本编译并在 VaM 中正常打开，再进行布局改造。不要在同一次提交里同时做大规模移动和交互行为修改。

### 5.2 建立最小 UI 状态模型

在 UI 文件中集中定义旧 Unity/C# 可编译的简单类型，避免使用项目可能不支持的新语法：

- `UiSection`：当前一级导航。
- `UiSelectionKind`：`None / Package / Scene / LocalPreset / VarPreset / Wearable`。
- `UiSelection`：统一持有选择类型和现有对象引用；取代多个 `selected*` 字段互相覆盖的隐式约定。
- `UiDensity`：桌面与 VR 的字号、间距、目标尺寸、卡片宽高和每页上限。
- `UiRefs`（或同等字段分组）：保存应用栏、浏览区、检查器、状态栏、设置层等引用，避免 `ClosePanel()` 手工遗漏清空。

持久状态与临时状态分开：

- 继续持久化：`uiScale`、`uiDistance`、`uiYOffset`、每页数量、每个导航的页码、自动化设置。
- 仅本次面板：搜索词、二级筛选、当前选择、检查器滚动位置、设置抽屉开关。
- 选择恢复以稳定 key 为准（包 UID；场景/包内预设用包 UID + entry path；本地预设用完整路径），不要按对象引用或列表下标恢复。

### 5.3 明确单向刷新流程

把 `RefreshList()`、`RefreshPresetList()`、`RefreshSceneList()`、`RefreshWearableList()` 中分散的状态决策统一为：

1. 根据当前导航、二级筛选和搜索生成一个结果集合。
2. 修正页码，但不在刷新时自动选中第一页第一项。
3. 尝试按稳定 key 保留原选择；若该项不在结果中，则清空选择。
4. 渲染当前页卡片。
5. 由统一的 `RenderInspector()` 根据 `UiSelectionKind` 重建/显隐右侧内容。
6. 更新结果计数、分页、空状态和状态栏。

打开面板时默认展示场景页可以保留，但应处于“尚未选择”状态。用户必须明确点击卡片后才能加载/应用；这项规则专门防止刷新后右侧按钮作用于意外对象。

## 6. 各区域的具体改造

### 6.1 应用栏

- 左侧固定显示 `AllPackagesLinker`，动态的包数/页数移到结果工具条，不再挤入标题。
- 中间搜索框占主要宽度，提示词随导航变化，例如“搜索场景名、包名或作者”。
- 搜索框加入清空按钮；输入时保留当前选择，只有选择不再匹配时才清空。
- 右侧只放设置、维护/重新扫描、关闭。重新扫描应显示忙碌态并防止重复触发。
- 一级分类从顶栏移到左侧导航或顶栏第二行；不要再把 10 个标签压进 50% 宽度。

### 6.2 导航与筛选

- 左侧导航使用带文本的纵向按钮，选中项使用强调色侧边条/底色，未选中项保持低对比。
- 当前分类下方显示二级筛选，不支持筛选的页面不创建占位条。
- 将 `Favorites` 的 `All / Scenes / Looks / Scripts / Presets` 逻辑保留，但修正 `Looks` 与 `Presets` 都显示“预设”的歧义，建议显示为“外观包”和“本地/包内预设”。
- “其他”作为 `全部` 页的分类筛选而非一级导航；若实际数据证明使用频率很高，再恢复独立入口。
- 二级筛选变化时恢复该筛选自己的页码；搜索变化时回到第一页。

### 6.3 结果工具条与卡片网格

- 工具条显示当前分类、结果总数、可选的来源筛选，以及桌面端“紧凑/舒适”密度或每页数量入口。
- 卡片统一为“缩略图 + 两行主标题 + 一行来源/作者 + 状态角标”；长 UID 不应承担唯一标题，保留完整值供悬停/详情显示。
- 卡片整块点击只负责选择；移除卡片底部同时出现的“加载/链接、收藏、保留”三按钮。若必须保留快捷操作，只保留右上角收藏星标，且点击不能改变选择后立即执行其他动作。
- 选中态使用边框/强调色和清晰底色，不只依赖非常接近的浅色。
- 缩略图加载继续沿用现有逐帧协程与缓存，先渲染占位骨架，再异步填图；切页/切分类继续取消旧协程并释放纹理。
- 分别提供“没有内容”“没有搜索结果”“正在扫描/建立索引”“缩略图不可用”的明确状态，不用空白网格代替。
- 桌面参考分辨率建议卡片最小宽 170–190 px；VR 使用较少列、更大字号和至少 44 px 的点击目标。不要把桌面的 500 项/页直接映射到 VR。

### 6.4 上下文检查器

检查器固定包含四层：预览、身份信息、上下文选项、动作区；只渲染与选择类型相关的层。建议映射如下：

| 选择类型 | 身份信息 | 上下文选项 | 主动作 | 次动作 |
| --- | --- | --- | --- | --- |
| 无选择 | 引导文案 | 无 | 禁用 | 无 |
| 场景 | 场景名、包、作者/版本、依赖状态 | 无 | 加载场景 | 收藏、仅链接、Hub |
| 本地/包内预设 | 名称、类型、来源 | 目标人物、应用范围 | 应用到人物 | 收藏、打开目录/包 |
| 服装/头发/形态 | 名称、来源包 | 目标人物（仅需要时） | 应用 | 收藏、仅链接、查看包 |
| 脚本 | 脚本名、来源包 | 目标原子 | 加载到原子 | 收藏、仅链接、Hub |
| 普通包/资源 | UID、分类、路径、依赖摘要 | 无 | 链接包 | 默认保留、Hub、更多 |

- `AtomRow`、预设包含服装/头发选项和三种快捷模式只在可应用预设时出现。
- “应用预设”“加载脚本”“加载场景”不可同时常驻；由选择类型决定唯一主按钮。
- Hub 检查/下载应在发现缺失依赖后展开；平时只显示依赖摘要和“检查依赖”。
- 下载进行时，将该区域切换为进度、速度/剩余时间、当前文件和取消按钮，避免在另一个固定区重复显示。
- 详情文本按字段分行并支持完整路径/描述的折叠；不要把所有信息拼成一个长字符串后截断。
- 操作进行中禁用冲突按钮；成功/失败结果同时写入检查器附近反馈和底部状态栏。

### 6.5 设置与维护

- 设置抽屉分为“常规”“扫描与加载”“VR 界面”“维护”四组。
- `autoOpenPanelInEditMode`、`autoAllowAllPlugins`、`scanAllPackagesOnStartup`、`autoCleanLinksBeforeSceneLoad` 保留原键名和默认值。
- VR 界面组仅在 VR 模式显示：缩放、距离、高度使用减号/当前值/加号组合，并提供“重新居中”“恢复默认”。
- 页容量改为预设值（如桌面 32/64/128，VR 24/48/64），替代无上下文的 ±8；仍可读取旧 `pageSize` 并夹取到合法值。
- “清除生成链接”和“删除真实包”置于维护组底部并使用危险色；删除真实包的确认框必须显示完整包名与真实路径，明确它不同于清除链接。

### 6.6 状态与分页

- 将底部两条分页区域合并为一条：`上一页`、最多 7 个页码、`下一页`、`第 x/y 页`。首末页通过窗口首尾或紧凑按钮访问。
- 移除常驻的 ±5/±10 与 20 个页码按钮；超多页时页码窗口使用省略号。
- 状态栏按信息/成功/警告/错误使用有限颜色语义，并保留最近一条完整消息的展开入口；不要只用 `OneLine(status, 120)` 静默截断关键错误。
- 扫描、下载、加载等异步任务显示可辨识的进行中状态；任务完成后刷新数据，但不要自动改变当前选择。

## 7. 分阶段实施顺序

### 阶段 A：建立安全基线

1. 记录桌面与 VR 当前界面截图，以及场景、预设、脚本、普通包、收藏五条核心流程。
2. 备份现有 DLL 和配置/收藏数据，确认当前 `_build/refs.rsp` 构建命令可重复执行。
3. 将 UI 代码拆到 partial class 文件并更新两个响应文件；此阶段只移动代码，要求行为和外观不变。

### 阶段 B：统一状态和刷新

1. 引入 `UiSelectionKind`、稳定选择 key 和统一选择清理方法。
2. 合并列表刷新管线，删除各 `Refresh*List()` 自动选中第一页第一项的行为。
3. 增加无选择、无结果、扫描中和错误状态；先验证刷新/搜索/翻页不会误操作，再改外观。

### 阶段 C：重建主框架

1. 按应用栏、导航、结果区、检查器、状态栏搭建新骨架。
2. 把设置和维护操作迁入抽屉；把 VR 调节迁入 VR 设置组。
3. 合并分页并建立桌面/VR 两套密度 token。

### 阶段 D：上下文检查器与卡片

1. 按选择类型实现检查器显隐与唯一主动作。
2. 统一卡片结构，移除重复动作，保留异步缩略图加载与资源释放。
3. 加入按钮禁用态、任务进行态、缺失依赖展开和明确错误反馈。

### 阶段 E：收尾与兼容

1. 保持旧配置键可读；需要新增键时提供默认值，不迁移或覆盖用户文件。
2. 更新 README 的界面说明、入口、设置位置和危险操作说明。
3. 完成桌面与 VR 回归测试后再替换正式 DLL；每阶段独立提交，便于回退。

## 8. 验证清单

### 构建与启动

- 使用 `_build/refs_test.rsp` 和 `_build/refs.rsp` 分别编译，确认无缺失源文件、旧 C# 语法或 Unity API 兼容错误。
- VaM 启动无新增异常；F7/F8、VR 手势打开/关闭面板正常，重复开关不残留 Canvas、纹理或协程。

### 核心工作流

- 场景：搜索 → 选择 → 检查依赖 → 加载；确认执行的始终是可见选中项。
- 预设：选择本地与包内预设，切换目标人物和应用范围后应用。
- 脚本/服装/头发/形态：只出现适用动作，且正确作用于目标原子。
- 普通包：链接、默认保留、Hub 详情、缺失依赖下载均保持原行为。
- 收藏：各资源类型收藏/取消后正确刷新，切换收藏子类保留各自页码。

### 状态边界

- 在搜索、切分类、翻页、重新扫描、下载完成后，选择保留或清空符合统一规则，绝不自动换成另一个资源。
- 空结果、损坏缩略图、缺失依赖、Hub 无地址、取消下载和业务异常都有可见反馈。
- 扫描/下载期间冲突按钮禁用，重复点击不会启动第二个任务。

### 视觉与 VR

- 在 1920×1080、常见宽屏分辨率和 UI 缩放下检查文字截断、遮挡、滚动与网格换列。
- VR 中从正常观看距离验证文字可读、主要触点不小于建议尺寸、射线可点击、滚动稳定。
- 设置抽屉、确认弹层和下载进度不会被列表遮挡；关闭后重新打开仍保持持久设置。

### 数据与危险操作

- 使用旧版 `config.tsv`、收藏和默认保留文件启动，确认数据完整。
- “清除生成链接”只处理 `_AllPackagesLinkerLinks` 且跳过默认保留；“删除真实包”显示完整路径并要求单独确认。
- 测试失败时只回退对应 UI 阶段，不使用会覆盖用户配置、收藏或包库的回退方式。

## 9. 推荐优先级

如果只能先做一轮，优先完成阶段 A–C，并在阶段 D 先覆盖场景与预设。最大收益来自三点：取消刷新自动选择、右栏按上下文显隐、设置/维护退出主工作流。配色和圆角属于最后润色，不应先于这三项。
